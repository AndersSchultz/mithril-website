#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mithril/bundler/bin/bundle" "$@"
  ret=$?
else 
  node  "$basedir/../mithril/bundler/bin/bundle" "$@"
  ret=$?
fi
exit $ret
